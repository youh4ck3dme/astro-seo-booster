---
const { hideHeader = false } = Astro.props;

const services = [
  {
    title: "Sťahovanie bytov a domov",
    desc: "Kompletné sťahovanie v Bratislave – balenie, demontáž, prevoz, montáž.",
    icon: "🏠",
    href: "/stahovanie",
  },
  {
    title: "Sťahovanie firiem a kancelárií",
    desc: "Firemné sťahovanie s minimálnym výpadkom prevádzky.",
    icon: "🏢",
    href: "/stahovanie",
  },
  {
    title: "Vypratávanie a odvoz odpadu",
    desc: "Vypratávanie bytov, pivníc, garáží a odvoz do zberného dvora.",
    icon: "📦",
    href: "/stahovanie",
  },
  {
    title: "Upratovanie a čistenie",
    desc: "Jednorazové aj pravidelné upratovanie domácností a firiem.",
    icon: "✨",
    href: "/stahovanie",
  },
];
---

<section class="services-preview">
  <div class="container">
    {!hideHeader && (
      <div class="header">
        <h2 class="title">Naše služby</h2>
        <p class="subtitle">Profesionálne riešenia pre každú situáciu</p>
      </div>
    )}

    <div class="grid">
      {services.map((service) => (
        <a href={service.href} class="service-card group">
          <div class="icon-wrapper group-hover:scale-110 group-hover:rotate-6 transition-all duration-500">
            {service.icon}
          </div>
          <h3 class="service-title">{service.title}</h3>
          <p class="service-desc">{service.desc}</p>
          <div class="card-footer mt-auto pt-6 border-t border-gray-100 flex items-center justify-between">
            <span class="learn-more">Zistiť viac</span>
            <span class="arrow transition-transform group-hover:translate-x-2">→</span>
          </div>
        </a>
      ))}
    </div>
  </div>
</section>

<style>
  .services-preview {
    padding: 64px 0;
  }
  .container {
    max-width: 1120px;
    margin: 0 auto;
    padding: 0 16px;
  }
  .header {
    text-align: center;
    margin-bottom: 48px;
  }
  .title {
    font-size: 36px;
    font-weight: 800;
    margin-bottom: 8px;
    color: #111827;
  }
  .subtitle {
    font-size: 18px;
    color: #6b7280;
  }
  .grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
    gap: 24px;
  }
  .service-card {
    background: white;
    padding: 40px;
    border-radius: 32px;
    text-decoration: none;
    color: inherit;
    box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.05), 0 2px 4px -1px rgba(0, 0, 0, 0.03);
    border: 1px solid #f1f5f9;
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    display: flex;
    flex-direction: column;
    height: 100%;
    position: relative;
    overflow: hidden;
  }
  .service-card:hover {
    transform: translateY(-12px);
    box-shadow: 0 30px 60px -12px rgba(15, 23, 42, 0.12);
    border-color: rgba(6, 95, 70, 0.1);
  }
  .icon-wrapper {
    width: 72px;
    height: 72px;
    background: linear-gradient(135deg, #f8fafc 0%, #f1f5f9 100%);
    border: 1px solid #e2e8f0;
    border-radius: 20px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 36px;
    margin-bottom: 32px;
    box-shadow: inset 0 2px 4px 0 rgba(255, 255, 255, 0.5);
  }
  .service-title {
    font-size: 24px;
    font-weight: 800;
    margin-bottom: 16px;
    color: #111827;
    letter-spacing: -0.02em;
  }
  .service-desc {
    font-size: 16px;
    color: #4b5563;
    line-height: 1.7;
    margin-bottom: 0;
    flex-grow: 1;
  }
  .learn-more {
    font-weight: 700;
    color: #065f46;
    font-size: 14px;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }
  .arrow {
    font-size: 20px;
    color: #065f46;
  }
</style>
