// Database initialization and seeding
// Now using localStorage - this file is kept for compatibility but does nothing

export async function initializeDatabase() {
  console.log("✅ Using localStorage - database initialization handled by storage module");
  return Promise.resolve();
}
